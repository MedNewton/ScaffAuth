# Examples

This directory provides reproducible CLI commands for common stacks.

- `basic-hono/`: default local backend
- `fastify-2fa/`: Fastify with 2FA and stronger session caching
- `express-rbac/`: Express with RBAC and email verification
